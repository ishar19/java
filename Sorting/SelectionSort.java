package javaCode.Sorting;


//pick largest or smallest element and put it at its correct place
//Stable ==> NO
public class SelectionSort {
    public static void main(String[] args) {
        
    }
    static void sort(int[] nums){
        for(int i=0; i<nums.length; i++){
            //find max in remaining array and swap with correct index
            int index = nums.length - i;
            // int maxIndex = getMaxIndex(nums, 0, index);
            // swap(index,getMaxIndex)
        }
    }
}
